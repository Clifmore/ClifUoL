# ClifUoL Project

Jupyter notebooks with Git integration.

## Setup
1. Install dependencies: `pip install -r requirements.txt`
2. Launch Jupyter: `jupyter notebook` or `jupyter lab`

## Git Integration
This project has Jupyter Git extensions installed.
